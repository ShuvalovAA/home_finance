{{- if ne .Values.deployment "production" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "service.fullname" . }}-postgresql-replica-2-env
  labels:
    {{- include "service.labels" . | nindent 4 }}
data:
  # POSTGRESQL settings
  POSTGRES_PASSWORD: "{{ .Values.postgresql.user.password }}"
  HFIN_POSTGRESQL_MASTER_PORT: "{{ .Values.postgresql.port }}"
  HFIN_POSTGRESQL_MASTER_HOST: "{{ .Values.postgresql.host }}"
  HFIN_POSTGRESQL_MASTER_PASSWORD: "{{ .Values.postgresql.user.password }}"
  HFIN_POSTGRESQL_MASTER_USER: "{{ .Values.postgresql.user.name }}"
  HFIN_POSTGRESQL_MASTER_DATABASE: "{{ .Values.postgresql.db.name }}"
  POSTGRES_PASSWORD: "{{ .Values.postgresql_replica_2.user.password }}"
  HFIN_POSTGRESQL_PORT: "{{ .Values.postgresql_replica_2.port }}"
  HFIN_POSTGRESQL_HOST: "{{ .Values.postgresql_replica_2.host }}"
  HFIN_POSTGRESQL_PASSWORD: "{{ .Values.postgresql_replica_2.user.password }}"
  HFIN_POSTGRESQL_USER: "{{ .Values.postgresql_replica_2.user.name }}"
  HFIN_POSTGRESQL_DATABASE: "{{ .Values.postgresql_replica_2.db.name }}"
{{- end }}
