apiVersion: v1
data:
  postgresql.conf: " \n wal_level = logical \n
    listen_addresses = '*' \n
    max_replication_slots = 20 \n
    max_logical_replication_workers = 10 \n
    max_worker_processes = 13 \n
    "

kind: ConfigMap
metadata:
  name: {{ include "service.fullname" . }}-postgresql-conf-master-env
