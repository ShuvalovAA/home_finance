apiVersion: v1
kind: Service
metadata:
  labels:
    app: kib-manual
  name: {{ include "service.fullname" . }}-kibana
  namespace: hfin
spec:
  ports:
  - port: 5601
    protocol: TCP
    targetPort: 5601
    nodePort: 21646
  selector:
    app: kib-manual
  type: NodePort
